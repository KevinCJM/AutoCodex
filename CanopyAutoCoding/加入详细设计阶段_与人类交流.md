- 发现的问题
  - `A00_main.main [总流程入口]`
    - 当前只串联 `A01_Routing_LayerPlanning.py [路由初始化阶段]`、`A02_RequirementIntake.py [需求录入阶段]`、`A03_RequirementsClarification.py [需求澄清阶段]`、`A04_RequirementsReview.py [需求评审阶段]`。
    - 详细设计在入口里仍然是“占位”，没有真实运行阶段。
  - `A04_RequirementsReview.py [需求评审阶段]`
    - 已经具备可复用骨架：复用/重建 `RequirementsAnalystHandoff [需求分析师运行句柄]`、并行审核、`check_reviewer_job [审核产物补救检查]`、`task_done [汇总评审并判定通过]`。
    - 这些骨架足以承接详细设计阶段，但原始需求里仍有几条关键规则没有落到唯一实现。
  - `Prompt_05_DetailedDesign.py [详细设计提示词集合]`
    - `create_detailed_design_ba [详细设计前置初始化]`、`detailed_design [生成详细设计文档]`、`review_detailed_design [详设评审]`、`modify_detailed_design [根据评审修改详设]`、`hitl_relpy [HITL 后继续修改]`、`again_review_detailed_design [二次评审]` 已存在。
    - 但仓库里没有对应的详细设计运行阶段文件，也没有接入总流程与 TUI 动作。

- 缺失的信息
  - `run_detailed_design_stage [拟新增详细设计阶段主流程]`
    - 当详细设计评审全部通过后，本次需求是否只需要把流程推进到“任务拆分阶段（待接入）”，还是必须继续接入真实的任务拆分运行阶段？
    - 当前仓库只有 `Prompt_06_TaskSplit.py [任务拆分提示词]`，没有对应的任务拆分运行阶段文件。
  - `run_detailed_design_stage [详细设计阶段的非交互决策]`
    - 如果从 `A04_RequirementsReview.py [需求评审阶段]` 自动进入详细设计，且命令使用 `--yes` 或处于非交互模式，“是否重建需求分析师”的默认规则是什么？
    - 原始需求要求“让人类选择”，但现有 CLI/TUI 需要一个确定的非交互分支。
  - `build_detailed_design_reviewers [详设审核器配置]`
    - 如果人类没有手工指定评审角色与角色提示词，系统默认创建哪些审核角色？
    - 至少需要几个审核器？是否存在必选角色，例如开发工程师、测试工程师、审核员？
  - `reviewer_name [审核器命名规则]`
    - `{角色定位}-{角色名称}` 里的 `角色名称` 由谁提供？
    - 是人类手工输入，还是系统自动编号，例如 `开发工程师-R1`、`测试工程师-R2`？
    - 这个规则会直接影响 `name_详设评审记录_{智能体名称}.md` 和对应 JSON 文件命名。
  - `check_reviewer_job [审核产物修复检查]` / `task_done [统一通过判定]`
    - 详设评审的单审核器 JSON 文件名，是否沿用现有 `name_评审记录_{智能体名称}.json` 契约，还是改成 `name_详设评审记录_{智能体名称}.json`？
    - 当前 `Prompt_05_DetailedDesign.review_detailed_design [详设评审提示词]` 的默认 JSON 名称，与现有工具使用的 JSON 模式不一致。
  - `run_detailed_design_stage [直接跳入详细设计阶段]`
    - 如果未执行 `A04_RequirementsReview.py [需求评审阶段]` 而直接进入详细设计，`name_需求澄清.md [需求澄清文档]` 是否必须已经存在？
    - 若缺少该文档，是直接阻断报错，还是自动回退到 `A03_RequirementsClarification.py [需求澄清阶段]`？

- 建议/影响
  - `A00_main.py [总流程入口]`
    - 如果不先确认“是否连带接入任务拆分阶段”，详细设计阶段的完成边界无法确定，容易把本次需求做大。
  - `A05 详细设计阶段 [拟新增阶段]`
    - 如果不先确认 `--yes` / 非交互时的默认决策，自动化流程无法稳定运行，CLI 与 TUI 会出现不一致分支。
  - `详设审核器配置 [人类可配置角色与提示词]`
    - 如果不先确认默认角色集与命名来源，文件命名、并行审核初始化、后续 repair loop 都无法唯一实现。
  - `详设评审 JSON 命名 [评审状态契约]`
    - 如果不先统一 JSON 文件命名，`check_reviewer_job [审核产物修复检查]` 和 `task_done [统一通过判定]` 的复用方式无法确定。
  - `直接跳入详细设计 [阶段入口校验]`
    - 如果不先确认前置文档要求，入口校验逻辑会在“直接报错”和“自动回退补阶段”之间产生歧义。
